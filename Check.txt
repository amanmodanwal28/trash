#!/bin/bash

# Source and destination paths
SRC="$HOME/Downloads/IOB_Server"
DEST="/home/ptcs/iob/bin"

# Check if source exists
if [ -d "$SRC" ]; then
    echo "Moving contents from $SRC to $DEST ..."

    # Create destination if it doesn't exist
    mkdir -p "$DEST"

    # Move all contents (files + folders) from source to destination
    mv "$SRC"/* "$DEST"/

    echo "✅ All contents moved successfully."
else
    echo "❌ Source folder does not exist: $SRC"
fi





#!/bin/bash

SRC="$HOME/Downloads/IOB_Server"
DEST="/home/ptcs/iob/bin"

if [ -d "$SRC" ]; then
    mkdir -p "$DEST"
    mv "$SRC"/* "$DEST"/
    echo "✅ All data moved from $SRC to $DEST"
else
    echo "❌ Source not found: $SRC"
    if [ -d "$DEST/IOB_Server" ]; then
        echo "ℹ️ Folder already exists in destination."
    else
        echo "ℹ️ Folder not found in destination."
    fi
fi
